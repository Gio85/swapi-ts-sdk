export enum HOST {
  API = 'https://swapi.dev/api',
}

export enum HTTP_METHOD {
  GET = 'get',
}

export enum ENDPOINT {
  PEOPLE = 'people',
  PLANETS = 'planets',
  FILMS = 'films',
  SPECIES = 'species',
  VEHICLES = 'vehicles',
  STARSHIPS = 'starships',
}

export enum FORMAT {
  WOOKIEE = 'wookiee'
}
