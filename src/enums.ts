export enum HOST {
  API = 'https://swapi.dev/api',
}

export enum HTTP_METHOD {
  POST = 'post',
  GET = 'get',
  PUT = 'put',
  PATCH = 'patch',
}

export enum ENDPOINT {
  PEOPLE = 'people',
  PLANETS = 'planets',
  FILMS = 'films',
  SPECIES = 'species',
  VEHICLES = 'vehicles',
  STARSHIPS = 'starships'
}