export enum HOST {
  API = 'http://localhost:3000',
}

export enum HTTP_METHOD {
  GET = 'get',
}

export enum ENDPOINT {
  CHARACTERS = 'characters',
  PLANETS = 'planets',
  FILMS = 'films',
  SPECIES = 'species',
  VEHICLES = 'vehicles',
  STARSHIPS = 'starships',
  SEARCH = 'search',
}

